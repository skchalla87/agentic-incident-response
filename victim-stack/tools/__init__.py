"""Host-side tooling for driving and inspecting the victim stack.

Stdlib only. These modules talk HTTP to Prometheus and Toxiproxy, shell out to
docker compose, and read the JSON log files off the bind mount.
"""
